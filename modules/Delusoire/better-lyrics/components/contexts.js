import { createContext } from "https://esm.sh/@lit/context";
export const scrollTimeoutCtx = createContext("scrollTimeout");
export const scrollContainerCtx = createContext("scrollContainer");
export const loadedLyricsTypeCtx = createContext("loadedLyricsType");
